# inbox

Drop Sony card JPEGs here (`_SA9*.JPG`). Optional DJI stills in a `DJI Drone Card Dump/` subfolder.

Ignore `.ARW`. Do not edit files in place.

```bash
python3 scripts/watch_inbox.py inbox --out jobs/listing --once
```

Official Drive INBOX: see `FOLDERS.md`.
